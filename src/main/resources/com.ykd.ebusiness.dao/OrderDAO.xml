<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youkeda.application.ebusiness.dao.OrderDAO">

    <resultMap id="OrderResultMap" type="com.youkeda.application.ebusiness.dataobject.OrderDO">
        <id column="id" property="id"/>
        <result column="gmt_created" property="gmtCreated"/>
        <result column="gmt_modified" property="gmtModified"/>
        <result column="order_no" property="orderNo"/>
        <result column="buyer_id" property="buyerId"/>
        <result column="seller_id" property="sellerId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="receiver_address" property="receiverAddress"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,gmt_created,gmt_modified,order_no,buyer_id,seller_id,product_id,product_name,product_image,price,quantity,total_amount,status,receiver_name,receiver_phone,receiver_address
    </sql>

    <insert id="insert" parameterType="com.youkeda.application.ebusiness.dataobject.OrderDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ykd_ebusiness_order (
        gmt_created, gmt_modified, order_no, buyer_id, seller_id, product_id,
        product_name, product_image, price, quantity, total_amount, status,
        receiver_name, receiver_phone, receiver_address
        ) VALUES (
        now(), now(), #{orderNo}, #{buyerId}, #{sellerId}, #{productId},
        #{productName}, #{productImage}, #{price}, #{quantity}, #{totalAmount}, #{status},
        #{receiverName}, #{receiverPhone}, #{receiverAddress}
        )
    </insert>

    <select id="selectById" resultMap="OrderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ykd_ebusiness_order
        WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE ykd_ebusiness_order
        SET status = #{status}, gmt_modified = now()
        WHERE id = #{id}
    </update>

    <select id="selectByBuyer" resultMap="OrderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ykd_ebusiness_order
        WHERE buyer_id = #{buyerId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY gmt_created DESC
    </select>

    <select id="selectBySeller" resultMap="OrderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ykd_ebusiness_order
        WHERE seller_id = #{sellerId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY gmt_created DESC
    </select>

</mapper>
